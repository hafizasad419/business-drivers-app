{
    "version": 2,
    "builds": [
        {
            "src": "/backend/src",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "/api/$1"
        }
    ]
}
